import { useEffect } from "react";

const AdminRedirect = () => {
  useEffect(() => {
    window.location.replace("https://rkconstruction-af.vercel.app/");
  }, []);

  return null;
};

export default AdminRedirect;
